/** @type {import('tailwindcss').Config} */
const config = {
  content: [
    "./src/*.js", // apne project ke files ke paths daalein
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

module.exports = config;
